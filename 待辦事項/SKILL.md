---
name: 待辦事項
description: "待辦事項管理工具。追蹤所有開發任務的進度與狀態，支援新增、更新、查詢待辦。當用戶說「/待辦事項」、「/待辦」、「/todo」、「還有什麼沒做完」、「待辦有哪些」時觸發。對話過程中發現新的待辦事項時主動提醒加入。"
---

# Skill: 待辦事項管理

## Usage Trigger

- 當用戶輸入 `/待辦事項`、`/待辦`、`/todo` 時觸發
- 當用戶詢問「還有什麼沒做完」、「待辦有哪些」、「進度」時觸發
- 當對話中發現新的待辦事項時，**主動提醒**用戶是否加入清單

---

## 1. 待辦檔案位置

待辦事項儲存於**目前專案目錄下**的 `memory/` 資料夾，可隨 git 版控同步到其他電腦：

```
<專案根目錄>/memory/todo_backlog.md
```

### 1.1 路徑規則

- **相對路徑**：永遠使用專案根目錄相對路徑 `memory/todo_backlog.md`，不使用絕對路徑
- **專案根目錄判定**：以當前 Claude Code 對話的 cwd 為準（通常是 git repo root）
- **禁止使用 `~/.claude/` 下的全域路徑**：全域路徑無法跨電腦同步，已廢棄

### 1.2 自動建立目錄

首次讀取或寫入 `memory/todo_backlog.md` 時，若 `memory/` 目錄不存在，**必須自動建立**：

```bash
mkdir -p memory
```

### 1.3 Git 版控建議

`memory/todo_backlog.md` 應加入 git 追蹤，這樣：
- ✅ 跨電腦同步待辦狀態（git clone / pull 後自動取得最新待辦）
- ✅ 多人協作時可透過 PR 審閱待辦變更
- ✅ 歷史追蹤（可看到待辦的新增/完成時間點）

**不建議**在 `.gitignore` 中排除 `memory/`，除非此目錄含有敏感資訊。

### 1.4 遷移舊版待辦

若偵測到舊版全域位置仍存在待辦：

```
~/.claude/projects/<專案名>/memory/todo_backlog.md
```

應**主動提醒使用者**：

```
💡 偵測到舊版全域待辦（~/.claude/projects/.../memory/todo_backlog.md）
是否要將內容遷移到專案目錄的 memory/todo_backlog.md？（推薦：可跨電腦同步）
```

使用者同意後：
1. 讀取舊版檔案內容
2. 寫入新路徑 `<專案>/memory/todo_backlog.md`
3. 刪除舊版檔案（或保留作為備份，由使用者決定）

---

## 2. 讀取待辦流程

### 2.1 讀取檔案

讀取 `todo_backlog.md`，以表格形式呈現所有待辦事項。若檔案不存在，視為空清單。

### 2.2 呈現格式

依優先順序排列，標注各項目的狀態與提醒：

```
| # | 項目 | 模組 | 狀態 | 提醒 |
|---|------|------|------|------|
```

**狀態圖示：**
- ⬜ 待辦
- 🔄 進行中
- ✅ 完成

**提醒圖示：**
- 🔴 阻塞中（有相依性，需先完成其他項目）
- 🟡 可開始（條件已具備）
- 🟢 簡單（預估 < 30 分鐘可完成）

### 2.3 呈現後建議

讀取完畢後，主動建議下一步應做哪個項目（優先選 🟡 可開始 或 🟢 簡單的）。

---

## 3. 新增待辦流程

### 3.1 主動偵測

在一般對話中，若出現以下情境，**必須主動提醒**用戶是否加入待辦清單：

- 用戶說「這個等等再做」、「先不處理」、「之後要改」
- 分析報告中列出尚未實作的項目
- 開發過程中發現需要額外處理的事項
- Code Review 發現需修正但本次不修的項目

提醒格式：
```
💡 偵測到新的待辦事項：「{項目描述}」
要加入待辦清單嗎？
```

### 3.2 新增格式

新增待辦時，必須包含：

| 欄位 | 說明 | 必填 |
|------|------|------|
| 項目 | 一句話描述任務 | ✅ |
| 模組 | 所屬模組或專案 | ✅ |
| 說明 | 詳細說明，含技術細節 | ✅ |
| 優先順序 | 🔴 高 / 🟡 中 / 🟢 低 | ✅ |
| 建立日期 | 當天日期 | ✅ |

---

## 4. 更新待辦流程

### 4.1 完成任務時

當對話中完成某項待辦任務後：

1. 將該項目狀態更新為 ✅
2. 移至「已完成」區塊
3. 記錄完成日期
4. 輸出提示：`✅ 待辦 #N「{項目}」已完成`

### 4.2 任務進行中

當開始處理某項待辦時：

1. 將狀態更新為 🔄 進行中
2. 不移動位置

---

## 5. 待辦清單結構規範

```markdown
## 🔴 高優先（阻塞其他工作）
| # | 項目 | 模組 | 說明 | 狀態 | 提醒 |

## 🟡 中優先（可開始）
| # | 項目 | 模組 | 說明 | 狀態 | 提醒 |

## 🟢 低優先（nice-to-have）
| # | 項目 | 模組 | 說明 | 狀態 | 提醒 |

## ✅ 已完成
| # | 項目 | 完成日期 |
```

---

## 6. Skill 串接機制

### 6.1 從其他 Skill 接收待辦

| 來源 Skill | 觸發情境 | 動作 |
|-----------|---------|------|
| 分析 | 分析報告中列出待確認/待實作項目 | 提醒加入待辦 |
| 開發-* | 開發中發現需額外處理的事項 | 提醒加入待辦 |
| CodeReview | 審查發現建議項但本次不修 | 提醒加入待辦 |
| Commit / 上版 | 版控完成後有後續任務 | 提醒加入待辦 |

### 6.2 觸發其他 Skill

當用戶選擇開始處理某個待辦項目時，建議對應的 Skill：

- 若項目需要分析 → 建議 `/分析`
- 若項目需要 Java 後端開發 → 建議 `/開發-Java後端`
- 若項目需要 Python 後端開發 → 建議 `/開發-Python後端`
- 若項目需要 React 前端開發 → 建議 `/開發-React前端`
- 若項目需要 Thymeleaf 前端開發 → 建議 `/開發-Thymeleaf前端`
- 若項目需要重構 → 建議 `/重構`
